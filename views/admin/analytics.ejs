<%- include('../partials/head', { title }) %>
<%- include('../partials/navbar', { user }) %>
<%- include('../partials/flash') %>

<div style="max-width:1200px;margin:0 auto;">
  <h1 style="font-size:26px;font-weight:800;color:#f8fafc;margin:0 0 24px 0;letter-spacing:-0.5px;">System Analytics</h1>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <div class="card">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;">
        <div style="width:4px;height:24px;background:linear-gradient(180deg,#c4b5fd,#7c3aed);border-radius:2px;"></div>
        <h2 style="font-size:18px;font-weight:700;color:#f8fafc;margin:0;">Quiz Activity (Last 30 Days)</h2>
      </div>
      <canvas id="activityChart" height="200"></canvas>
    </div>

    <div class="card">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;">
        <div style="width:4px;height:24px;background:linear-gradient(180deg,#93c5fd,#2563eb);border-radius:2px;"></div>
        <h2 style="font-size:18px;font-weight:700;color:#f8fafc;margin:0;">User Role Distribution</h2>
      </div>
      <canvas id="roleChart" height="200"></canvas>
    </div>

    <div class="card">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;">
        <div style="width:4px;height:24px;background:linear-gradient(180deg,#6ee7b7,#10b981);border-radius:2px;"></div>
        <h2 style="font-size:18px;font-weight:700;color:#f8fafc;margin:0;">Top Modules by Enrollments</h2>
      </div>
      <canvas id="modulesChart" height="200"></canvas>
    </div>

    <div class="card">
      <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;">
        <div style="width:4px;height:24px;background:linear-gradient(180deg,#fcd34d,#f59e0b);border-radius:2px;"></div>
        <h2 style="font-size:18px;font-weight:700;color:#f8fafc;margin:0;">Average Quiz Scores by Module</h2>
      </div>
      <canvas id="scoresChart" height="200"></canvas>
    </div>
  </div>
</div>

<script>
const COLORS = ['#6366f1','#8b5cf6','#06b6d4','#10b981','#f59e0b','#ef4444','#ec4899','#84cc16'];

const actData  = <%- JSON.stringify(activityByDay) %>;
new Chart(document.getElementById('activityChart'), {
  type: 'line',
  data: {
    labels: actData.map(d => new Date(d.day).toLocaleDateString('en-GB', {month:'short',day:'numeric'})),
    datasets: [{ label: 'Quiz Attempts', data: actData.map(d => d.quiz_count),
      borderColor: '#6366f1', backgroundColor: 'rgba(99,102,241,0.1)', fill: true, tension: 0.4 }]
  },
  options: { plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true } } }
});

const roleData = <%- JSON.stringify(roleDistrib) %>;
new Chart(document.getElementById('roleChart'), {
  type: 'doughnut',
  data: {
    labels: roleData.map(r => r.role.charAt(0).toUpperCase() + r.role.slice(1)),
    datasets: [{ data: roleData.map(r => r.count), backgroundColor: COLORS }]
  },
  options: { plugins: { legend: { position: 'bottom' } } }
});

const modData  = <%- JSON.stringify(topModules) %>;
new Chart(document.getElementById('modulesChart'), {
  type: 'bar',
  data: {
    labels: modData.map(m => m.module_name),
    datasets: [{ label: 'Enrollments', data: modData.map(m => m.enrollments),
      backgroundColor: '#6366f1', borderRadius: 6 }]
  },
  options: { plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true } } }
});

const scoreData = <%- JSON.stringify(avgScores) %>;
new Chart(document.getElementById('scoresChart'), {
  type: 'bar',
  data: {
    labels: scoreData.map(s => s.module_name),
    datasets: [{ label: 'Avg Score %', data: scoreData.map(s => s.avg_score),
      backgroundColor: scoreData.map(s => s.avg_score >= 70 ? '#10b981' : s.avg_score >= 50 ? '#f59e0b' : '#ef4444'),
      borderRadius: 6 }]
  },
  options: { plugins: { legend: { display: false } }, scales: { y: { beginAtZero: true, max: 100 } } }
});
</script>

<%- include('../partials/footer') %>